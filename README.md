# Northsignal Labs — MSP/Cybersecurity Template Micro-Assets

Northsignal Labs is a small independent automation lab building no-spend, practical cybersecurity/MSP templates and local tools. This repository-style index is designed to be copied into a free static publishing route later without exposing any private individual or personal account.

> Operational-use notice: these assets are templates and automation aids. They are not legal, insurance, brokerage, compliance, audit, certification, Microsoft-affiliation, or professional-services advice.

## Start here: full pack + request path

- **Use the complete pack first:** start with the highest-intent MSP monthly report asset (`msp-monthly-security-report-template.html`) and then pull the matching Markdown templates, schemas, samples, and local generators listed below.
- **Request the next useful template after launch:** if this pack is published through an approved non-personal GitHub channel, use `.github/ISSUE_TEMPLATE/template-request.yml` to ask for a new template or generator without sharing client names, private security details, credentials, personal contact details, or confidential information.
- **Validate future money without payment setup:** if public launch later attracts useful visitors, `.github/ISSUE_TEMPLATE/commercial-fit-signal.yml` can collect non-binding commercial-fit signal about which expanded pack, generator bundle, starter kit, workflow, value band, and monetization trigger might be worth pursuing later, while prohibiting client names, private/security-sensitive details, personal contact details, contract terms, quote/invoice requests, and confidential information.
- **Keep feedback safe and measurable:** `CONTRIBUTING.md` and `.github/ISSUE_TEMPLATE/config.yml` make future GitHub feedback structured by disabling blank issues and routing public visitors to template-request or commercial-fit signal forms that prohibit sensitive data.
- **Keep aggregate signal countable:** `.github/labels.yml` predefines the `template-request`, `commercial-fit`, and `demand-signal` labels used by the issue templates and aggregate snapshot helper, and `GITHUB-LABEL-SETUP.md` gives a post-approval label setup handoff so an approved GitHub launch does not lose demand signal because labels were missing.
- **Keep approved GitHub Pages launch low-friction:** `.github/workflows/static-pages.yml` is a post-approval-only static Pages deploy workflow, and `tools/validate_github_pages_workflow.py` verifies it stays limited to GitHub Pages official actions with no secrets, analytics, payment, contact-capture, or paid-hosting references.
- **Keep repository metadata copy/paste-safe:** `REPOSITORY-METADATA.md` and `REPOSITORY-METADATA.json` provide matching post-approval repository display name, description, topics, pinned launch copy, issue/label/Pages settings, aggregate metrics, and no-go workflows so an approved launch is not delayed by retyping metadata.
- **Keep security feedback public-safe:** `SECURITY.md` tells future visitors not to post client names, credentials, vulnerability details, logs, screenshots, contact details, quote/payment data, or other sensitive information, and makes clear there is no private vulnerability inbox or bounty workflow in this no-spend static package.
- **Give visitors a browser-friendly start guide:** `download.html` maps the fastest value path, what to open first, supporting files for each workflow, and which aggregate/public-safe signals to watch after an approved launch; `DOWNLOAD.md` keeps the same guidance in Markdown.
- **Make generator value obvious before install/run friction:** `generator-quickstart.html` gives a browser-friendly one-command proof path for regenerating all five sample reports and links each generator to its schema, sample JSON, generated HTML/Markdown output, and approved-launch request path.
- **Show proof before script execution:** `sample-outputs.html` gives browser-friendly previews and `SAMPLE-OUTPUTS.md` links the generated Markdown/HTML demos for the MSP monthly report, NIS2 readiness summary, M365 Secure Score executive report, cyber-insurance gap register, and vCISO QBR summary so future visitors can judge value quickly before running local tools.
- **Regenerate all proof outputs with one local command:** `tools/run_all_sample_generators.py` reruns all five JSON-to-Markdown/HTML sample generators and verifies the output files, so a future reviewer or visitor can prove the full pack works without hunting through five separate commands.
- **Make request intent browser-friendly:** `request-signal.html` explains which public-safe template-request or commercial-fit path to use after an approved launch, what aggregate category signal can be counted, and what sensitive/contact/payment data must never be collected.
- **Keep every HTML entry routed to signal:** all five asset landing pages now link directly to `download.html`, `sample-outputs.html`, `request-signal.html`, and the template-request/commercial-fit workflows, and `tools/validate_html_conversion_routes.py` fails if those conversion paths disappear.
- **Make the next request easy to classify:** `REQUEST-ROADMAP.md` lists candidate next templates/generator improvements and maps each to public-safe `template-request` or `commercial-fit` signal so the first approved launch can reveal the highest-ROI follow-up workflow without forms, analytics, contact capture, payment setup, or private-data collection.
- **Measure week-one validation deliberately:** `FIRST-7-DAY-VALIDATION-PLAN.md` turns the first approved public week into day-by-day aggregate-signal checks, one careful free-channel share rule, and explicit no-spend next-action thresholds.
- **Use an approved GitHub Pages runbook:** `APPROVED-GITHUB-PAGES-LAUNCH-RUNBOOK.md` gives the post-approval day-0 sequence, repository setup checklist, gate commands, first aggregate measurement commands, and stop conditions for a concrete non-personal GitHub Pages launch.
- **Collapse approved launch preflight into one local command:** after concrete approved URL/account fields pass validation, `tools/run_approved_launch_preflight.py --apply-cutover` runs the guarded public URL cutover for `sitemap.xml`, `robots.txt`, and schema `$id` references, then final gates, repo-root export, ZIP packaging, and dashboard honesty checks without uploading or creating accounts.
- **Smoke-test the live URL after reviewer-controlled upload:** `tools/smoke_test_public_url.py` stays blocked until approved public URL fields exist, then checks an expanded allowlist of launch-critical public HTTPS paths — entry files, start/download guides, AI/search summary, checksum sheet, all five landing pages, generated sample previews, robots/sitemap, labels, and request/commercial-fit templates — for 200 responses, placeholder leakage, forms, analytics, and payment/contact-capture risk before the first aggregate-signal snapshot.
- **Keep AI/search summaries accurate:** `llms.txt` gives future crawlers, AI answer engines, and visitors a concise map of the pack, best entry points, safe request paths, and no-spend/no-data/no-payment guardrails so approved public discovery can route to useful assets without inventing claims or collecting data.
- **Keep upload integrity checkable:** `CHECKSUMS.txt` lists SHA-256 hashes and byte counts for the manifest-approved public files, and `tools/write_release_checksums.py` regenerates it before final gates so a future approved reviewer can spot stale or incomplete upload trees before traffic validation starts.
- **No monetization friction yet:** there are no forms, analytics scripts, payment links, account signups, commercial partner links, or contact collection in this local package; first validation is limited to aggregate public stars, forks, watchers, open issues, and labeled `template-request`/`commercial-fit` counts after an approved release.
- **Clear reuse permission after reviewer approval:** this package now includes `LICENSE` with the approved MIT License under Northsignal Labs attribution, while the operational-use disclaimers below still apply.

## First publish batch

| Priority | Asset | Format | Why it matters | Local path |
|---:|---|---|---|---|
| 1 | Free MSP Monthly Security Report Template | Landing page + Markdown + JSON schema + generator | Highest current score; concrete MSP reporting need; now includes local JSON-to-Markdown/HTML automation for repeat monthly reports. | `msp-monthly-security-report-template.html`, `tools/msp_monthly_security_report_generator.py` |
| 2 | NIS2 Readiness Checklist for MSP Clients | Landing page + Markdown + JSON schema + generator | Strong evidence-collection intent; now includes a local JSON-to-Markdown/HTML evidence-summary generator while keeping NIS2 applicability/legal conclusions out of scope. | `nis2-readiness-checklist.html`, `tools/nis2_readiness_summary_generator.py` |
| 3 | M365 Secure Score Executive Report Template | Landing page + Markdown + JSON schema + generator | Automation differentiator: converts structured JSON into Markdown/HTML report output. | `m365-secure-score-executive-report-template.html`, `tools/m365_secure_score_report_generator.py` |
| 4 | Cyber Insurance Evidence Checklist for MSPs | Landing page + Markdown + JSON schema + generator | Renewal/application evidence gathering is urgent and MSP-adjacent; now includes a local questionnaire-to-gap-register generator. | `cyber-insurance-evidence-checklist.html`, `tools/cyber_insurance_gap_register_generator.py` |
| 5 | vCISO QBR Agenda Template for MSPs | Landing page + Markdown + JSON schema + generator | Completes governance/reporting bundle with reusable QBR agenda, executive scorecard, decisions requested, and local QBR summary generator. | `vciso-qbr-agenda-template.html`, `tools/vciso_qbr_summary_generator.py` |

## Suggested free publishing sequence

1. Publish the static files only after an approved no-spend channel exists that can use the Northsignal Labs identity without exposing any private individual or personal account.
2. Start with the MSP monthly report and NIS2 checklist pages because they are easiest to understand and require no tool execution.
3. Add the NIS2 readiness summary, M365, cyber-insurance gap-register, and vCISO QBR summary generators as differentiation hooks: "download a template, or generate a local report from JSON."
4. Do not add payment, quote, invoice, contact, partner/referral, or sales paths until aggregate public demand signal exists and a separate monetization workflow is explicitly approved.
5. Track aggregate public signals first: stars, forks, watchers, open issues, `template-request` label counts, and `commercial-fit` label counts. Do not use analytics/page-view scripts, referrer logs, forms, private inboxes, issue bodies, usernames, emails, IPs, or client-sensitive details as validation data.

## Local usage quick links

- Open `index.html` for the local static homepage.
- See `download.html` for the browser-friendly start/download guide, and `DOWNLOAD.md` for the Markdown version that routes MSP, NIS2, M365, cyber-insurance, and vCISO visitors to the right asset and future public-safe signal path.
- See `generator-quickstart.html` for the browser-friendly generator proof path that shows the one-command `tools/run_all_sample_generators.py` workflow plus the five generator/schema/sample/output link sets.
- See `llms.txt` for the public-safe AI/search discovery summary that lists the highest-value entry points, local tools, request paths, and guardrails without requiring analytics, forms, external calls, payment links, or a public URL guess.
- See `CHECKSUMS.txt` for SHA-256/byte-count integrity metadata for the manifest-approved public files, and `tools/write_release_checksums.py` to regenerate it before an approved upload review.
- See `sample-outputs.html` for browser-friendly sample previews, `SAMPLE-OUTPUTS.md` for generated Markdown/HTML sample links, and `tools/run_all_sample_generators.py` for a one-command local proof run that regenerates all five sample outputs before visitors inspect or fork the pack.
- See `request-signal.html` for the browser-friendly public-safe request/commercial-fit signal guide that turns approved-launch interest into aggregate category counts without forms, analytics, contact capture, payment setup, or private-data collection.
- See `REQUEST-ROADMAP.md` for public-safe candidate next requests and aggregate thresholds that keep post-launch follow-up work tied to revenue validation instead of low-ROI polish.
- See `asset-scores.json` for scoring and next actions.
- See `PUBLISHING-ROUTES.md` for zero-cost distribution options and risk notes.
- See `PUBLICATION-CHECKLIST.md` for the pre-publication privacy, claims, no-spend, and channel approval review gate.
- See `GITHUB-PAGES-SETUP.md` for privacy-safe static repository setup notes for a future approved non-personal free channel.
- See `SIGNAL-TRACKING.md`, `manual-aggregate-counters.example.json`, `asset-signal-scores.example.json`, `tools/collect_public_signal_snapshot.py`, and `tools/decide_next_revenue_validation_action.py` for lightweight post-launch aggregate signal tracking — stars, forks, watchers, open issues, template-request counts, commercial-fit counts, and optional 0..3 aggregate asset scores only, with a guarded manual aggregate mode for approved non-GitHub/equivalent free channels — plus a no-spend next-action decision so weak, some, or meaningful signal leads to distribution, conversion, or monetization-learning work rather than low-ROI polish or premature payment setup.
- See `FIRST-7-DAY-VALIDATION-PLAN.md` for the post-approval day 0/day 1/day 7 checklist that converts launch into aggregate demand validation, one rules-compliant free-channel share only when appropriate, and threshold-based next actions before any monetization workflow.
- See `APPROVED-GITHUB-PAGES-LAUNCH-RUNBOOK.md` for the post-approval GitHub Pages day-0 runbook that ties approved URL fields, public URL reference cutover, repository metadata, label setup, workflow validation, ZIP packaging, upload guardrails, and first aggregate-signal collection into one launch sequence.
- See `tools/run_approved_launch_preflight.py` for the post-approval one-command local preflight that validates the approved fields, dry-runs/applies public URL reference cutover only when requested, reruns gates, prepares `public-repo-export/`, packages the ZIP, and writes `APPROVED-LAUNCH-PREFLIGHT.json` while keeping upload/publication reviewer-controlled.
- See `tools/smoke_test_public_url.py` for the post-upload approved-HTTPS smoke check that confirms the live static paths load and do not expose placeholders, forms, analytics, payment links, or contact-capture before aggregate signal collection.
- See `LICENSE` for the approved MIT License covering the public release files under Northsignal Labs attribution.
- See `.nojekyll` for the GitHub Pages compatibility marker that keeps the future static release from being processed by Jekyll and reduces last-minute deployment surprises on the approved free channel.
- See `RELEASE-MANIFEST.json` for the include/exclude list for a future static release.
- See `RELEASE-SUMMARY.md` for a reviewer-friendly staged package summary with commands, expected counts, guardrails, and unresolved blockers.
- See `RELEASE-RISK-DIGEST.md` for the condensed blocker, publishing-prerequisite, telemetry, and regression-state checkpoint before any approved public release.
- See `RELEASE-DECISION-NOTE.md` for the explicit local hold/go decision aid that maps aligned release gates to reviewer criteria before any approved public upload.
- See `DISTRIBUTION-READINESS-SCOREBOARD.md` for a local comparison of free publishing/distribution routes against the release-decision go/no-go criteria.
- See `DISTRIBUTION-MESSAGE-BANK.md` for post-approval, public-safe launch/release/community copy that is kept local until a non-personal no-spend channel is approved.
- See `REPOSITORY-METADATA.md` and `REPOSITORY-METADATA.json` for copy/paste-safe repository description, topics, homepage policy, release-copy, first-validation measurement guidance, issue/label/Pages settings, and forbidden pre-approval workflows to shorten the approved GitHub Pages/static-channel launch handoff without creating accounts, uploads, analytics, payment links, or contact capture.
- See `SECURITY.md` for public-safe sensitive-data and vulnerability-reporting boundaries that reduce reputational/data-intake risk before any approved GitHub release.
- See `PUBLIC-URL-CUTOVER-CHECKLIST.md` for the local-only host replacement and final gate checklist to use only after a free non-personal public channel and URL are approved.
- See `APPROVAL-HANDOFF.md` for the exact reviewer-supplied channel, public URL, metadata, no-spend, financial-workflow, data-collection, and claims/identity details required before Hermes can perform the local URL cutover.
- See `APPROVAL-HANDOFF-FIELDS.json`, `tools/apply_public_channel_fields.py`, `tools/validate_approval_handoff.py`, `tools/perform_public_url_cutover.py`, and their disposable-fixture tests for the machine-readable approval preflight, reviewer-supplied field application, and validator-gated local public URL reference cutover that stays blocked until the required reviewer fields are complete and proves unsafe future approval records fail closed.
- See `.github/ISSUE_TEMPLATE/template-request.yml` for a future GitHub-native, public-safe demand-validation hook that asks users which template/generator would help most while warning them not to share client names, private security details, credentials, personal contact details, or confidential information.
- See `.github/ISSUE_TEMPLATE/commercial-fit-signal.yml` for a future public-safe, non-binding commercial-fit signal hook that helps decide whether an expanded pack, generator bundle, starter kit, workflow pack, or pilot path deserves monetization work after launch without creating a sale, payment flow, quote, contact collection, or confidential-data intake.
- See `CONTRIBUTING.md` and `.github/ISSUE_TEMPLATE/config.yml` for public-safe contribution rules and issue-template routing that keep future validation structured while blocking blank issues likely to collect sensitive data.
- See `.github/labels.yml`, `GITHUB-LABEL-SETUP.md`, and `tools/validate_github_signal_labels.py` to confirm and set up the future GitHub labels needed for aggregate template-request and commercial-fit measurement before launch.
- See `.github/workflows/static-pages.yml` and `tools/validate_github_pages_workflow.py` for the post-approval GitHub Pages deployment handoff that can publish the static repository root only after the approved non-personal repository/URL exists; the validator is filesystem-only and blocks paid-hosting, secrets, analytics, payment, and contact-capture references.
- See `tools/validate_public_launch_packet_sync.py` for the filesystem-only guardrail that verifies `PUBLIC-LAUNCH-APPROVAL-PACKET.md` still matches the latest verified `GO-LIVE-PACKAGE.json` ZIP file count, byte size, and SHA-256 before reviewer approval against the one-screen packet.
- See `tools/validate_html_conversion_routes.py` for the filesystem-only guardrail that keeps all public HTML entry and asset pages linked to the start/download guide, sample-output proof, request-signal guide, and structured request/commercial-fit workflows before launch.
- See `tools/validate_public_release_bundle.py` for the filesystem-only bundle-completeness guardrail that confirms the manifest-approved package still includes the visitor start guide, license/reuse files, human-readable and machine-readable repository metadata, `CHECKSUMS.txt` integrity metadata, structured request/commercial-fit hooks, aggregate-signal tooling, post-upload live URL smoke testing, GitHub Pages handoff, approved-launch preflight helper, and public-safe contribution/security boundaries while keeping internal approval packets and generated artifacts out of the public manifest.
- See `robots.txt`, `sitemap.xml`, and the generator JSON schemas for local public URL placeholders that must keep the placeholder host until a concrete public URL is approved.
- See `CHANGELOG.md` for local release notes for the first MSP Security Reporting Template Pack.
- See `tools/README.md` for the MSP monthly, NIS2, M365, cyber-insurance, and vCISO QBR generator commands, `tools/run_all_sample_generators.py` for regenerating every sample output in one local proof run, `tools/apply_public_channel_fields.py` for dry-running/applying explicitly supplied public-channel fields into the handoff file, `tools/validate_approval_handoff.py` for approval-field preflight, `tools/perform_public_url_cutover.py` for validator-gated public URL reference replacement after approval, `tools/run_approved_launch_preflight.py` for the one-command post-approval local preflight/gate/export/package sequence, `tools/test_approval_handoff_validator.py` and `tools/test_perform_public_url_cutover.py` for disposable safety tests, `tools/smoke_test_public_url.py` for the post-upload approved-HTTPS reachability and guardrail smoke check, `tools/collect_public_signal_snapshot.py` for future aggregate-only public signal snapshots, `tools/decide_next_revenue_validation_action.py` for translating aggregate signal into the next no-spend revenue-validation action, `tools/validate_github_signal_labels.py` for verifying public signal labels are declared, `tools/validate_github_pages_workflow.py` for verifying the post-approval Pages workflow guardrails, `tools/validate_public_launch_packet_sync.py` for confirming the launch approval packet matches the latest verified ZIP handoff, `tools/pre_publication_check.py` for local release checks, `tools/stage_release.py` for staging manifest-approved files into `dist/`, and `tools/review_staged_release.py` for comparing the staged release against the source manifest before review.

## No-spend guardrails

- Do not buy domains, hosting, ads, tooling, subscriptions, or marketplace placements.
- Do not create payout, tax, KYC, or paid-service commitments autonomously.
- Do not use any private individual's personal name, accounts, or background in public materials without explicit approval.
- Do not invent a human founder. If attribution is needed, describe Northsignal Labs as a small independent automation lab.
